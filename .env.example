# Google Drive API Configuration (for local development)
# For GitHub Pages deployment, these are injected via GitHub Secrets during build

# OAuth 2.0 Client ID from Google Cloud Console
# NOTE: Safe to expose in client-side code - restrict by "Authorized JavaScript origins"
VITE_GOOGLE_CLIENT_ID=your_client_id_here.apps.googleusercontent.com

# Google API Key from Google Cloud Console
# NOTE: Safe to expose in client-side code - restrict by "HTTP referrers" in console
VITE_GOOGLE_API_KEY=your_api_key_here

# Setup Instructions:
# 1. Go to https://console.cloud.google.com/
# 2. Create/select project and enable "Google Drive API"
# 3. Configure OAuth consent screen (External type)
# 4. Create OAuth 2.0 Client ID:
#    - Application type: Web application
#    - Authorized JavaScript origins:
#      * http://localhost:5173 (local dev)
#      * https://yourusername.github.io (production)
# 5. Create API Key and restrict by HTTP referrer:
#    - http://localhost:5173/* (local dev)
#    - https://yourusername.github.io/* (production)
# 6. For local dev: Copy to .env (gitignored)
# 7. For GitHub Pages: Add as repository secrets (Settings > Secrets > Actions)

# GitHub Actions Build Example:
# - name: Build
#   run: npm run build
#   env:
#     VITE_GOOGLE_CLIENT_ID: ${{ secrets.VITE_GOOGLE_CLIENT_ID }}
#     VITE_GOOGLE_API_KEY: ${{ secrets.VITE_GOOGLE_API_KEY }}
